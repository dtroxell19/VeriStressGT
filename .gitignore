__pycache__/
*.py[cod]
*.swp
.venv/
.env
build/
dist/
*.egg-info/
.ipynb_checkpoints/

# Artifacts
*.onnx
*.pt
*.npy
*.npz

# Keep toy example assets tracked
!examples/toy_weights.pt
!examples/toy_instance.npz
!examples/toy_spec.vnnlib

# Benchmark / run artifacts (generated at evaluation time)
*_bench/
*_run/
*_run */
results.json

# Pre-built mini_sweep benchmark committed for reproducibility
!mini_sweep_bench/
!mini_sweep_bench/**

# Machine-local environment reconstruction, written by scripts/run_veristressgt_native.sh
# and aiq/run_mini_sweep_env.sh so a cmd_queue worker (which inherits nothing)
# can find a relocated conda. Never the same on two machines.
aiq/.conda_root
aiq/.shim/
